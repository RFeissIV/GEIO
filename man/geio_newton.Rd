% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/equilibrium.R
\name{geio_newton}
\alias{geio_newton}
\title{Newton's method for equilibrium systems}
\usage{
geio_newton(
  F,
  x0,
  DF = NULL,
  max_iter = 200L,
  tol = 1e-10,
  damping = 1,
  regularize = 1e-08,
  cond_max = 1e+12
)
}
\arguments{
\item{F}{Vector-valued function: \code{function(x) -> numeric vector}.}

\item{x0}{Initial guess, numeric vector.}

\item{DF}{Optional Jacobian function: \code{function(x) -> matrix}.
If \code{NULL}, \code{\link{geio_numjac}} is used.}

\item{max_iter}{Maximum Newton iterations (default 200).}

\item{tol}{Convergence tolerance on \eqn{\|F(x)\|_\infty}
(default \code{1e-10}).}

\item{damping}{Initial damping factor for backtracking (default 1.0).}

\item{regularize}{Tikhonov regularization added to diagonal when
the Jacobian condition number exceeds \code{cond_max}
(default \code{1e-8}).}

\item{cond_max}{Condition number threshold triggering regularization
(default \code{1e12}).}
}
\value{
A named list:
\describe{
  \item{\code{x}}{Numeric vector; the equilibrium point (or last iterate).}
  \item{\code{converged}}{Logical; \code{TRUE} if
    \eqn{\|F(x)\|_\infty < \text{tol}}.}
  \item{\code{residual}}{Scalar; \eqn{\|F(x)\|_\infty} at termination.}
  \item{\code{iterations}}{Integer; Newton iterations used.}
  \item{\code{jacobian}}{Matrix; Jacobian \eqn{D_x F} at the final
    iterate.}
}
}
\description{
Finds \eqn{x^*} such that \eqn{F(x^*) \approx 0} using damped
Newton iteration with backtracking.  If the Jacobian is singular or
near-singular, a Tikhonov regularization term is added.
}
\examples{
# Intersection of two parabolas: x^2 + y = 1, x + y^2 = 1
F <- function(x) c(x[1]^2 + x[2] - 1, x[1] + x[2]^2 - 1)
sol <- geio_newton(F, c(0.5, 0.5))
sol$x          # should be near (1, 0) or (0, 1)
sol$converged

}
